# YOLO 检测 App 版本规划与发布规范

制定日期：2026-09-16　当前版本：**1.2.1**

## 一、版本号序列（用户既定规划）

| 版本区间 | 系列 | 说明 |
|---|---|---|
| 1.2.1 – 1.9.8 | **YOLOv5-Lite 系列** | 基于现有 ncnn 模型（e / s / c 及量化版），逐步扩展能力 |
| 2.0.1 – 2.9.8 | **YOLOv8 系列** | anchor-free + DFL 解码 |
| 3.0.1 – 3.9.8 | **YOLO11 系列** | 与 v8 同构（DFL），复用同一解码 |
| 4.x / 5.x | 预留未启用 | — |
| 6.0.1 – 6.9.8 | **YOLO26 系列** | 无 DFL；ncnn 导出会禁用 end2end 分支，仍需 NMS |

规则：
- 主版本号区分系列（1 = YOLOv5-Lite，2 = YOLOv8，3 = YOLO11，6 = YOLO26）；
- 次版本号为功能迭代，修订号为缺陷修复；
- 版本号末位不使用 0（从 1 起步，如 1.2.1）；
- 每个系列内预留到 .9.8，接近上限时升主版本号。

> 多系列的实施路线、后处理差异与待决策项见 `多系列演进方案_v5-v8-v11-v26.md`。

## 二、两个发布平台

| 平台 | 产物 | 版本存放位置（单一事实来源） |
|---|---|---|
| Android | `releases/YOLOv5-Lite检测V<版本>_<日期>.apk` | `android_demo/.../app/src/main/AndroidManifest.xml` 的 `android:versionName`（同步更新 `versionCode`） |
| Windows | `releases/YOLOv5-Lite检测V<版本>_win64.zip` | `win_app/version.py` 的 `APP_VERSION` |

两端发版必须同一版本号。`关于` 页面分别读取各自的版本来源展示，并显示所属系列。

## 三、版本号改动清单（避免漏改）

1. Android：`AndroidManifest.xml` → `versionName` + `versionCode`（每次 +1）
2. Windows：`win_app/version.py` → `APP_VERSION`
3. 发布包文件名中的版本号
4. 本文「当前版本」一行

## 四、发布前自检（内容级，非仅看构建成功）

Android：
- `aapt dump badging` 确认 versionName / targetSdk
- 解压 APK，确认 `lib/*/libncnnv5lite.so` 含 `onDetected`、`nativeSetListener` 字符串（回调链路已编译）
- 确认 dex 中中文字符串为 UTF-8（GBK 命中 0）

Windows：
- `YOLOv5-Lite检测.exe --selftest`（无需摄像头）能加载模型、输出检出结果与记录路径

## 五、记录文件规范（两端一致）

目录：公共「下载」目录 `YOLOv5-Lite检测记录/`
文件：每天一个 `detect_YYYYMMDD.csv`，字段 `时间,物体,置信度`，UTF-8 BOM。
历史按天保留，不自动清理；清空操作只影响当天（删除全部需显式确认）。
