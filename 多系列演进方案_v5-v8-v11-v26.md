# YOLO 多系列演进方案（v5-Lite → v8 → v11 → v26）

制定日期：2026-09-16　状态：**规划阶段（未实施）**
当前版本：1.2.1（YOLOv5-Lite 系列，安卓 + Windows 双平台已交付）

> 用户要求：第四项先规划研究，方案成熟再实施，分步干。
> 本文件只做方案与规范对齐，**未改动任何模型、推理、后处理代码**。

---

## 一、版本 — 系列映射（已定）

| 主版本区间 | 系列 | 代表模型 | 状态 |
|---|---|---|---|
| 1.2.1 – 1.9.8 | **YOLOv5-Lite** | v5lite-e / s / c（及量化版） | ✅ 已交付 |
| 2.0.1 – 2.9.8 | **YOLOv8** | yolov8n / s | ⬜ 待实施 |
| 3.0.1 – 3.9.8 | **YOLO11** | yolo11n / s | ⬜ 待实施 |
| 4.x / 5.x | 预留未启用 | — | — |
| 6.0.1 – 6.9.8 | **YOLO26** | yolo26n / s | ⬜ 待实施 |

规则沿用：主版本号区分系列；次版本号为功能迭代，修订号为修复；末位不用 0；每系列内预留到 .9.8。

---

## 二、研究结论（2026-09-16 实测 + 检索）

### 2.1 可获得性：链路全通（已实测，非推测）

| 项 | 结论 | 证据 |
|---|---|---|
| ultralytics 版本 | 最新 **8.4.153**，含 YOLO26 | PyPI JSON 实测 |
| YOLO26 权重 | **可下载**，yolo26n.pt **5.5 MB** | `curl -I` GitHub release → HTTP 200 + Content-Length |
| pnnx（ncnn 转换） | **20260526**，有 `win_amd64` 轮子 | PyPI JSON 实测 |
| 导出链路 | `model.export(format="ncnn")`，ultralytics 内建，内部走 pnnx | 官方文档 |
| ncnn 推理 | 本机已装 ncnn 1.0.20260526（Python 轮子） | 上一轮已验证 | 
| 本机工具链 | 无 MSVC / cmake / MinGW / OpenCV C++ | 上一轮探测结论 |

**关键结论**：不需要 C++ 编译环境即可产出 ncnn 模型（ultralytics + pnnx 是 Python 工具链），Win 端直接复用现有 ncnn Python 轮子即可推理。**这条路是通的**。

### 2.2 后处理差异矩阵（**2026-09-16 已实测刷新，推翻原假设**）

> 原方案按「v8/v11 输出 144 通道、需在端上做 DFL 16-bin softmax」设计。
> **实测证明这是错的**——ultralytics 8.4.x 的 ncnn 导出会把 DFL、anchor 网格、stride 缩放、sigmoid
> **全部烘焙进计算图**，端上拿到的已是解码后的最终结果。

**实测数据（imgsz=320，ncnn 1.0.20260526，ultralytics 8.4.153）**：

| 系列 | reg_max | head cv2 / cv3 | ncnn `out0` 形状 | 行 0–3 语义 | 行 4–83 语义 |
|---|---|---|---|---|---|
| YOLOv8 (2.x) | 16 | 64 / 80 | **(84, 2100)** | `cx, cy, w, h`（320 空间绝对像素） | 80 类概率（**已 sigmoid**） |
| YOLO11 (3.x) | 16 | 64 / 80 | **(84, 2100)** | 同上 | 同上 |
| YOLO26 (6.x) | **1** | **4** / 80 | **(84, 2100)** | 同上 | 同上 |

三个系列的 ncnn 输出**完全同构**。`2100 = 40² + 20² + 10²`（stride 8/16/32）。

**v8/v11 的 DFL 去哪了**：`.param` 尾部可见 `Softmax → add/div/sub → mul(stride)` 链路，
即 16-bin 分布加权求和、`dist2bbox`、乘 stride 全在图内完成，输出直接是像素坐标。
所以**端上一行 softmax 都不用写**。

**结论 —— 三个新系列共用一个解码器**：

```python
cx, cy, bw, bh = out[0:4, i]          # 已是 320 空间绝对像素
x1 = cx - bw/2;  y1 = cy - bh/2
x2 = cx + bw/2;  y2 = cy + bh/2
score = out[4 + cls, i]               # 已 sigmoid，直接与阈值比较
# 仅剩两步：置信度过滤 + NMS + letterbox 逆变换回原图
```

这比原方案（端上实现 DFL softmax + anchor 网格 + stride 映射）**少一大半工作量**，
且 v8 / v11 / v26 **无需分别写解码**，一个 `decode_anchorfree()` 通吃。

**对拍验证**（zidane.jpg，1280×720，imgsz=320，conf=0.25）—— ncnn 解码 vs PyTorch 官方预测：

| 系列 | PyTorch 基准 | ncnn cxcywh 解码 | 吻合 |
|---|---|---|---|
| v8 | person .81 [146,201,933,720]；person .79 [739,38,1143,720] | person .82 [133,201,918,715]；person .80 [737,36,1145,709] | ✅ 2/2 |
| v11 | 3 框（.90/.85/.27） | 3 框（.89/.85/.32） | ✅ 3/3 |
| v26 | person .85 [744,41,1146,720]；person .84 [138,202,1097,720] | person .85 [744,43,1146,713]；person .85 [121,200,1053,714] | ✅ 2/2 |

（对照：`ltrb` 解码得 [50,-186,1361,1066]、`xyxy` 解码出现 y2<y1 —— 均错误，**已排除**。）

现状代码：
- Win 端：`win_app/detector.py` 的 `HEADS` 常量写死 stride/blob/anchors，`_decode()` 是 anchor 专用实现
- 安卓端：`jni/yolov5.cpp` 的 `generate_proposals()` 同样是 anchor 专用

**可复用**：letterbox 前处理（补 114）、1/255 归一化、NMS、COCO 80 类中文表、结果回调链路。
**必须抽象**：解码头（v5-Lite anchor-based 与新系列 anchor-free 两套）。

### 2.3 ⚠️ 关键发现：YOLO26 导出 ncnn 会丢掉 end2end

官方导出时有明确警告：

```
WARNING ⚠️ NCNN export does not support end2end models, disabling end2end branch.
```

含义：YOLO26 主打的「原生端到端、免 NMS」在 ncnn 路径下**不生效**，导出的是经典 one-to-many 头，**仍需 NMS**。

影响有两面：
- **坏处**：拿不到 v26 免 NMS 的部署简化红利，且转换收益打折。
- **好处**：ncnn 路径下 v26 的后处理**与 v8/v11 完全同构**，不必为它单写一套 NMS-free 逻辑。

> ~~**最大未知（P1 必须实测）**：YOLO26 去掉 DFL 后输出通道数是多少。~~
> **✅ P1 已完成（2026-09-16）**：v26 的 `reg_max=1`（DFL 退化为单 bin），**输出 84 通道**，
> 与 v8/v11 在 ncnn 下**完全一致**。原"v8/v11=144、v26=84"的推测**不成立**——三者都是 84。

### 2.3b ⚠️ 导出是**定尺寸**的 —— 影响「多档尺寸」方案（实测）

ncnn 导出会把输入尺寸烘焙进 `Reshape`（`0=N 1=1`），**一个模型文件只能吃一种输入尺寸**：

| 输入 | anchor 数 N | 说明 |
|---|---|---|
| 320 | 2100 | = 40²+20²+10² |
| 416 | 3549 | = 52²+26²+13² |
| 640 | 8400 | = 80²+40²+20² |

喂给 320 模型一张 640 的图，输出**仍是 2100**（不是 8400），结果无意义。

**所以「多档尺寸让用户选」= 每个尺寸各导出一份模型**，不是运行时改 resize。

体积实测（bin 与输入尺寸基本无关，因为权重相同）：

| 系列 | bin | 三档全出 | CPU 单帧（本机实测，320 / 640） |
|---|---|---|---|
| YOLOv8n | ≈12.1 MB | ≈36 MB | 9 ms / 33 ms |
| YOLO11n | ≈10.0 MB | ≈30 MB | 8 ms / 35 ms |
| YOLO26n | ≈9.2 MB | ≈28 MB | 9 ms / 35 ms |

> 注：CPU 耗时为本机 ncnn（无 Vulkan）实测，手机端会明显更快（v5lite 现在走的也是同路径）。
> 640 相比 320 慢约 4 倍，与算力平方关系一致。

**对决策 2/3 的影响**：
- 按需下载：单档 ≈9–12 MB，三档合计 ≈87 MB（三系列）。用户通常只下 1–2 个，可接受。
- 建议默认只打包 **320**（体积小、够快），416/640 作为可选下载项。

### 2.4 环境成本与隔离要求

- `ultralytics` 轮子仅 1.4 MB，但依赖 `torch`（win wheel 约 118 MB，若走默认 CUDA 依赖可能吃掉 2 GB+）
- **必须**用 CPU 版索引安装：`--index-url https://download.pytorch.org/whl/cpu`
- **必须**用独立 venv（如 `export_env/`），**严禁**装进 `win_app/.venv312`
  - 原因：`.venv312` 是 PyInstaller 打包环境的输入，混进 torch/ultralytics 会让 exe 体积从 107 MB 膨胀到 GB 级
- C 盘仅剩 20 GB，导出环境建议落在 D 盘

---

## 三、架构改造（一次性，为三个系列铺路）

改造目标：**加一个新系列 = 加一个模型文件 + 一份 manifest 条目 + （必要时）一个解码函数**，不改主流程。

### 3.1 后处理抽象

**实测后简化为两套**（原计划三套）：

```
Detector
├── preprocess()      # letterbox + 归一化（四系列通用，已存在）
├── inference()       # ncnn extract（按 manifest 取 blob 名）
└── decode_head()     # ★ 按 series 分派，只需两套
      ├── decode_v5lite(anchor)        # 1.x：三输出 85 通道，sigmoid + anchor/stride
      └── decode_anchorfree()          # 2.x/3.x/6.x 通吃：单输出 (84, N)
                                       #   cx,cy,w,h 已乘 stride；cls 已 sigmoid
```

`decode_anchorfree()` 实测可用的最小实现（Python 侧，C++ 同理）：

```python
def decode_anchorfree(out, conf_thr, nms_thr):
    # out: (84, N)  行0-3 = cx,cy,w,h（输入尺寸绝对像素）；行4-83 = 80 类概率(已sigmoid)
    cls = out[4:, :]
    score = cls.max(0); label = cls.argmax(0)
    keep_score = score > conf_thr
    cx, cy, bw, bh = out[0][keep_score], out[1][keep_score], out[2][keep_score], out[3][keep_score]
    boxes = np.stack([cx-bw/2, cy-bh/2, cx+bw/2, cy+bh/2], 1)
    idx = nms(boxes, score[keep_score], nms_thr)
    return boxes[idx], score[keep_score][idx], label[keep_score][idx]
# 之后按 letterbox 的 scale/pad 逆变换回原图坐标即可
```

### 3.2 模型清单元数据化

新增 `models/manifest.json`，替代现在硬编码的 `MODELS` 元组：

```json
{
  "series": "yolo26",
  "models": [
    {"label": "YOLO26n 320", "param": "yolo26n.param", "bin": "yolo26n.bin",
     "input": 320, "decode": "anchorfree", "classes": 80, "blobs": ["out0"]}
  ]
}
```

Win 端与安卓端**读同一份 manifest**，保证两端模型口径一致（这也是上一轮踩过的坑：两端各写一份容易漂移）。

### 3.3 UI 两级选择

现在 spinner 平铺 7 个 v5lite 模型。加系列后需改为：**系列选择 → 该系列下的模型选择**。

### 3.4 命名泛化（改动面大，建议单独立项）

现有命名全部绑死 v5lite：

| 位置 | 现在 | 建议 |
|---|---|---|
| 安卓包名 / JNI 类 | `ncnn.v5lite.demo` | `ai.vision.yolo`（影响 JNI 函数名 `Java_ncnn_v5lite_demo_*`，需同步改 `.so` 导出符号） |
| native 库 | `libncnnv5lite.so` | `libyoloncnn.so` |
| App 名 | YOLOv5-Lite 实时检测 | 随系列变化或改为中性名 |
| 记录目录 | `YOLOv5-Lite检测记录/` | 需保持向后兼容（旧记录不能丢） |

⚠️ 安卓包名变更 = **新应用**，用户升级会丢历史数据，且旧版无法覆盖安装。这是需要用户拍板的决策点。

---

## 四、分步实施计划

| 阶段 | 内容 | 产出 | 是否动发布代码 |
|---|---|---|---|
| **P0** | 规范对齐：版本映射表（四段） | 本文件 + 版本规划文档 + 关于页文案 | 仅文案 |
| **P1** | ✅ **已完成**：导出 venv → 导出 v8/v11/v26 → 实测 out0=(84,2100)、语义 cxcywh+已sigmoid → 与 PyTorch 对拍通过 | `export_env/recon/`（含 decode_verify.json） | 否 |
| **P2** | 抽象层改造（Win 端先行，成本低）：Detector 拆分 + manifest 驱动 | Win 端可跑多系列 | 是 |
| **P3** | 接入 YOLOv8 → 发 **2.0.1** | 双平台 2.0.1 | 是 |
| **P4** | 接入 YOLO11 → 发 **3.0.1**（**直接复用 v8 解码，零新增**） | 双平台 3.0.1 | 是 |
| **P5** | 接入 YOLO26 → 发 **6.0.1** | 双平台 6.0.1 | 是 |
| **P6** | （可选）命名泛化 / 多系列共存 | — | 大改 |

每阶段结束都必须双平台同步发版，不允许出现"Win 有、安卓没有"的漂移。

---

## 五、验证口径（内容级，不只看构建成功）

沿用本项目既定纪律（上一轮 v1.2 的教训：只验"类在包里"导致 native 回调失效没被发现）：

1. **形状实测**：导出后用 Python 打印每个输出 blob 的 `(c, h, w)`，写进 manifest，不许靠文档猜
2. **同图对比**：固定 `bike.jpg`，四系列各跑一次，记录检出类别/数量/置信度，横向对比
3. **与 PyTorch 对齐**：同一张图用 `ultralytics` 原模型跑一遍（阈值一致），ncnn 结果的框位置容差 ≤ 2 px、类别一致率 100%
4. **安卓端**：解压 APK 确认新 `.so` 符号 + dex 中文 UTF-8（GBK 命中 0）
5. **Win 端**：`--selftest` 能加载新系列模型并输出检出结果

---

## 六、决策结果（2026-09-16 已定，取代此前的待定清单）

| # | 议题 | 用户决策 | 落地方式 |
|---|---|---|---|
| 1 | App 名 / 包名 | **可以改名，接受改名导致的历史数据丢失** | 包名改为中性名，不再纠结覆盖安装兼容性；改造时可一并处理 `/opt`、记录目录等命名 |
| 2 | 输入尺寸 | **多档并存，由用户选择** | `manifest.json` 每个系列提供 320/416/640 多档，UI 下拉可选。<br>⚠️ **实测：ncnn 导出定尺寸，每档需单独导出一份模型**（见 2.3b）。建议默认 320 |
| 3 | 模型打包 | **按需下载，不全量打进安装包** | 单档 9–12 MB，三系列三档合计 ≈87 MB。安装包只带默认档，其余 App 内按需下载 + md5 校验 |
| 4 | 4.x / 5.x | **预留** | 主版本号 4、5 不启用，为后续未知系列留位 |
| 5 | v5-Lite 归属 | **新系列稳定后归档** | 1.x 停止功能迭代，代码保留但不再随新系列同步改动 |

> 决策 3 引出一项新增工作：需要一个**模型下载器 + 本地模型仓库 + 校验（md5/大小）**，
> 放在 P2 抽象层之后、P3 之前，否则 2.0.1 无法按需取模型。
> 决策 1 的改名涉及 `/opt` 路径与记录目录，需保证**旧记录的向后兼容读取**（否则用户历史记录丢失后无法找回）。

---

## 六点五、P1 侦察产出（2026-09-16 完成）

环境与脚本落在 `export_env/`（独立 venv，**未**污染 `win_app/.venv312`）：

| 文件 | 作用 |
|---|---|
| `export_env/recon_head.py` | 探测 head 结构（reg_max / cv2·cv3 通道 / end2end） |
| `export_env/recon_export_ncnn.py` | 导出 ncnn 并解析 `.param` |
| `export_env/recon_decode_verify.py` | ★ 与 PyTorch 对拍，判定输出语义 |
| `export_env/recon/head_report.json` | head 结构原始数据 |
| `export_env/recon/decode_verify.json` | ★ 三系列对拍结果 |
| `export_env/recon/export_matrix.json` | 三系列 × 三尺寸 导出矩阵 |
| `export_env/v{8,11,26}_{320,416,640}_ncnn/` | 已导出的 ncnn 模型（部分） |

环境：`torch 2.14.0+cpu`、`ultralytics 8.4.153`、`ncnn 1.0.20260526`。

> ⚠️ 复现注意：ultralytics 导出会清理中间文件，易触发本环境的**批量删除守卫**（每轮 50 文件阈值）。
> 一次只导一个模型、或分轮执行可绕过。

---

## 七、本轮已改动（仅规范对齐，零逻辑变更）

- `版本规划与发布规范.md`：系列映射扩展为四段（1.x/2.x/3.x/6.x）
- `win_app/version.py`：`series()` 按主版本号返回对应系列名
- `MainActivity.java` 的 `seriesName()`、两端「关于」文案：与新映射一致
- 当前版本号**仍为 1.2.1，未改动**
